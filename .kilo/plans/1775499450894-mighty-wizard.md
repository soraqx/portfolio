# Plan: Prepare Portfolio for Vercel Deployment

## Current State Analysis
- **Framework**: Vite + React 18
- **Styling**: Tailwind CSS
- **Existing files**: `package.json`, `vite.config.js`, `index.html`, `src/` components
- **Build output**: Already has `dist/` folder
- **Git ignore**: Only `.kilo/.gitignore` exists (wrong location for Vercel)

## Required Actions

### 1. Create Root `.gitignore`
Create a standard `.gitignore` at project root with Vercel-appropriate entries:
- `node_modules/`
- `dist/`
- `.env*` files
- Logs, IDE files, OS files

### 2. Create `vercel.json`
Add Vercel config for:
- SPA fallback to `index.html` (handles client-side routing)
- Proper headers for static assets

### 3. Verify/Update `vite.config.js`
Consider adding `base: './'` for relative paths (safer for subdirectory deployments)

### 4. Update `package.json` (Optional)
Add Vercel-specific scripts if needed:
- `"deploy": "vercel --prod"` (if Vercel CLI is used)

## Estimated Files to Create/Modify
- **Create**: `.gitignore`, `vercel.json`
- **Modify**: `vite.config.js` (optional), `package.json` (optional)

## Notes
- Vercel automatically detects Vite projects
- The project should work out of the box after adding `.gitignore`
- The `dist/` folder should NOT be committed (will be generated by Vercel)
